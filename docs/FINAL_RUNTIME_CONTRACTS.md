# @connectingmatrix/sockets final runtime contracts

This file is generated by the final process-monitor / project / nodes / workflow-AI / Giga-agents hardening pass.

## Ownership

- socket rooms and room subscriptions
- server/frontend broadcast bridge
- log and process-monitor event transport

## Public API contracts

- `Socket.register(room)`
- `Socket.regester(room) compatibility alias`
- `Socket.broadcast(room,payload,event,traceId)`
- `Socket.on(room,handler)`
- `Socket.emitLog(payload)`
- `Socket.emitProcess(payload)`
- `Socket.bindLogger(Logger)`

## Routes / launcher

- `GET /sockets/health`
- `GET /sockets/launcher`

## Notes

- Guards process-monitor/log broadcasts so observability events do not recursively re-enter socket tracking.

## Stub launcher

Run the local package launcher with:

```bash
npm run build
node playground.mjs
```
